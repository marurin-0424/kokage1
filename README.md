# こかげ

建物の影から、子連れの夏の外出を変える。

都知事杯オープンデータ・ハッカソン2026 応募作品（テーマ：気候変動）

公開ページ → **[index.html](./index.html)**

夏の午後、3〜5歳を連れてどこで遊ばせるか。天気予報は今日の暑さを、地図アプリは近い公園を教えてくれますが、**その公園まで歩く道のどこが日陰か**は、どちらも教えてくれません。こかげは、3D都市モデルから建物と高架の影を計算して、そこを埋めます。

対象は **池袋駅周辺 約2.5km × 1.1km**（歩行空間ネットワークデータの収録範囲）です。

---

## 画面

| ファイル | 中身 |
|---|---|
| **`kokage-v1b-mobile.html`** | **★ 最新。**4ステップ（ゲート → 時刻6枚 → 行き先3件 → 道）。時刻を切り替えると、公園の影の形と必要な水分量が同時に変わります。①は暑さ指数の目盛り、④は日なた／日陰を色分けした地図です |
| `kokage-v1-mobile.html` | v1b の1つ前（2026-08-17）。差分の比較用 |
| `kokage-v0-mobile.html` | 旧版（スマホ） |
| `kokage-v0.html` | 旧版（PC） |
| `walk-timer.html` | 3〜5歳を連れた歩行速度を測るツール |

いずれも **単一HTMLで、外部通信をしません。** CSS・JS・地図SVGをすべてインラインにしてあり、計算済みの答えをファイルに書き出す構成なので、計算用のサーバが要りません。

---

## 4つのステップ

1. **今日行けるか** — 暑さ指数31以上のときは行き先を出しません。幼児・学童には一つ上の温度基準域を当てます
2. **何時に出るか** — 11〜16時の6枚。待つ価値がある時刻と、**悪くなる時刻**の両方を出します
3. **どこへ行くか** — 候補3件を、公園の影の形とあわせて。**選ぶのは利用者です**
4. **どの道で行くか** — **ここだけは、こかげが決めます。**最短の道より 1.8% だけ遠回りすると、往復で日なたにいる時間が 31% 減ります（14時・n=400 の中央値。0.5分以上減るのは84%）

③で決めず④で決めるのは、精度の裏づけが違うからです。**道路の影は現地確認3回とも概ね一致しましたが、公園の影は 9〜32ポイント少なく見ています**（樹木が入っていないため）。

---

## 使っているオープンデータ

### 答えに反映しているもの（5件・いずれも加工して利用しています）

| データ | 提供 | ライセンス |
|---|---|---|
| 東京都3Dデジタルマップ（3D都市モデル 豊島区 2025年度版） | **東京都** | CC BY 4.0 |
| 歩行空間ネットワークデータ（池袋駅周辺） | 国土交通省 | 公共データ利用規約（第1.0版） |
| 公共施設一覧 | 豊島区 | **CC BY 2.1 日本** |
| 赤ちゃん・ふらっと一覧 | 東京都福祉局 | CC BY 4.0 |
| 熱中症予防情報（暑さ指数WBGT） | 環境省 | 公共データ利用規約（第1.0版） |

- 豊島区の表記は、区のオープンデータページが **CC BY 2.1 日本**、東京都カタログが CC BY 4.0 で食い違っています。**厳しい方（2.1 日本）に合わせています。**
- 赤ちゃん・ふらっと一覧は、都カタログのリソースリンク（令和7年1月版）が現在404のため、配布元である東京都福祉局のページから取得時点の最新版（令和8年6月17日現在）を使用しています。

### 検証にのみ使用（作品には組み込んでいません）

都道の街路樹（東京都建設局／CC BY 4.0）、アメダス観測データ（気象庁／公共データ利用規約 第1.0版）、緑のオープンデータ GISデータ（東京都都市整備局／ライセンス表記なし）、都営バス GTFS-JP（東京都交通局／CC BY 4.0）。

---

## 計算スクリプト

`src/` に置いてあります。依存は **shapely / pyproj / pvlib / networkx の4つだけ**です（手元で再現できることを設計目標にしたため）。座標系は EPSG:6677。CityGML は変換ツールを挟まず `zipfile` と正規表現で直接パースしています。

| ファイル | 役割 |
|---|---|
| `src/shadow.py` | 太陽位置と建物の影 |
| `src/bridge.py` | 高架デッキの影と、デッキ下 |
| `src/route.py` | 熱ストレスを重みにした経路探索 |
| `src/hydration.py` | 発汗量の推定 |
| `src/screen6.py` | 6時刻ぶんの答えを計算（`out/screen6.json`） |
| `src/park_svg.py` | 公園の影の形（`out/park_svgs.json`） |
| `src/hour_pick.py` | 時刻ごとの歩道の日陰率と、暑さ指数の実測集計 |
| `src/degrade.py` | 退化テスト（順位が距離順と一致していないかを測る） |
| `src/build_v1.py` | 画面HTMLの生成 |

`out/` は計算結果のJSONです。画面はここから生成されます。

```bash
cd src
PYTHONPATH=. python3 build_v1.py
```

---

## 前提と限界（自分から書いておきます）

- **日影は 2026年8月12日の太陽位置**による計算値で、**晴天時の値**です。曇りの日の補正は入っていません
- **公園の日陰に樹木は含まれていません**（建物と高架の影のみ）。現地確認では、実際より **9〜32ポイント少なく**見ていました。だから公園については**割合の数字を出さず、形だけを見せています**
- 暑さ指数は**入力値**です。時刻をずらしたときの値は、環境省の実測（2025年8月・都内3地点・31日分）から求めた時刻差を当てています
- 建物は LOD2 実形状585棟＋LOD1。**LOD1 の箱では上階がすぼむ形（斜線制限）を表現できず**、公園の日陰率が過大に出ます
- 歩行速度 53.1 m/分は、3〜5歳を連れた実測（n=3）です
- 保護者へのヒアリングは**1名**です

---

## ライセンス

- **コード（`src/`）**：MIT License（`LICENSE`）
- **`out/` のJSON**：上記オープンデータを加工して生成したものです。元データのライセンス（CC BY 4.0／CC BY 2.1 日本／公共データ利用規約 第1.0版）に従ってください
- 画面HTMLに埋め込まれている地図・図形も同様です

---

## 注意

**数値は公開データからの推定値であり、医学的な判断ではありません。**
お子様の様子を見て、のどの渇きに応じて自由に飲めるようにしてください（環境省）。
